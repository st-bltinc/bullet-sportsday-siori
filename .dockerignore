# ── Git・エディタ ─────────────────────────────────────────────
.git
.gitignore
.DS_Store
.claude

# ── 機密情報 ─────────────────────────────────────────────────
.env

# ── Node.js（全階層）────────────────────────────────────────
node_modules
*/node_modules

# ── ビルド済みファイル（builder ステージで生成するため除外）──
# ※ src/ public/ vite.config.* は builder ステージで必要なため除外しない
*/dist

# ── ログ・一時ファイル ───────────────────────────────────────
*.log
error_log
*/error_log

# ── スクリプト・ドキュメント（サーバー不要）────────────────
scripts/
output/
*.pptx
README.md

# ── SQLite データ（コンテナ内で初期化するため除外）──────────
common/database/
common/sessions/
